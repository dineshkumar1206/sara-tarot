// Central configuration for API URL
// Change this URL to your live backend server URL (e.g. 'https://api.saraatarot.com')
// when deploying, or set VITE_API_URL in a .env / .env.production file.
export const API_BASE_URL = import.meta.env.VITE_API_URL || 'http://localhost:5000';
